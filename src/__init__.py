"""
Core package for the Negative project.

This file is intentionally minimal; it just marks `src` as a package so that
imports like `from src.collectors import ...` work.
"""



























